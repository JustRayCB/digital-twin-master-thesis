/**
 * @fileoverview State configurations specific to the main application navigation header.
 */

import type { ViewState } from "$shared/types";

/** Defines the ordered list of primary views displayed in the navigation bar. */
export const navItems: ViewState[] = ["OVERVIEW", "ANALYTICS", "JOURNAL", "SETTINGS"];
